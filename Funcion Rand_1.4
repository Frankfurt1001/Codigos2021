#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#define C 36000
int main(int argc, char *argv[]) {
	srand(time(NULL));
	for(int i = 0; i < C; i++){
		printf("%d\n", 1+rand()%6);//esto imprimira numeros desde el 1 hasta el 6
	}
	return 0;
}

