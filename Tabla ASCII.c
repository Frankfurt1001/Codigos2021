#include <stdio.h>

int main(int argc, char *argv[]) {
	int var=32;
		for( var ; var<=256 ; var++){//ordenarlo con un for anidado luego
		printf(" El numero %d\t en ASCII seria: %c\n", var, var);//tabla ASCII que muestra desde el 32 hata el 256
	}
	
	
	
return 0;
}

