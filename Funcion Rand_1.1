#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
	srand(0);/*esta funcion se pone apenas empieza la funcion main
	dicha funcion necesita un valor de una semilla*/
	for(int i = 0; i < 10; i++){
	printf("%d\n", rand());
	}
	return 0;
}

