#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#define T 36000
int tiro_dados();
int carga();
int mostrar();
int tiro_total();
int main(int argc, char *argv[]) {
	srand(time(NULL));
	tiro_dados();
	mostrar();
	return 0;
}
int tiro_dados(void){
	int sumat = 0;
	int tirost[T]= {0};
	static int cant_tiros = 0;
	for(int i = 1; i <= T; i++){
		sumat=(1+rand()%6)+(1+rand()%6);//esto imprimira numeros desde el 1 hasta el 6
		tirost[sumat]++;
		cant_tiros++;
	}
	return tirost[T];
}
	int mostrar(void)
	{
		int cant_tiros;
		int tirost[T];
		printf("\n\t Suma de los dados\t\t Frecuencia\n");
		for(int i = 2; i <= 12; i++){
			printf("\t%9d\t\t|", i);
			printf("%18d\n\n", tirost[i]);
			
		}
		printf("\n Los tiros realizados fueron: %d", cant_tiros);
		return 0;
	}
	
