#include <stdio.h>
//puntero a funcion oara organizacion
void operacion( int (*p)(int, int), int a, int b){//el primer parametro es un puntero a funcion
	printf("El resultado es: %d\n", (*p)(a,b));
}
int suma (int a, int b){
	return a+b;
}
	int resta (int a, int b){
		return a-b;
	}
	int main(int argc, char *argv[]) {
	operacion(suma,3,4);
	operacion(resta,3,4);
		return 0;
	}
	
	
